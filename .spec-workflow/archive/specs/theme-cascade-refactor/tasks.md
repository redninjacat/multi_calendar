# Tasks Document: Theme Cascade Refactor

## Phase 1: Theme infrastructure

- [x] 1. Add new properties to MCalThemeData
  - File: lib/src/styles/mcal_theme.dart
  - Add `eventTileLightContrastColor` (Color?), `eventTileDarkContrastColor` (Color?), and `hoverEventBackgroundColor` (Color?) to MCalThemeData. Update constructor, `fromTheme` factory (light: `Colors.white`, dark: `colorScheme.onSurface` — full opacity per M3; hover: `colorScheme.primaryContainer.withValues(alpha: 0.8)`), `copyWith`, `lerp`, `==`, `hashCode`. Update dartdoc with "master defaults" terminology. Note: `cellBorderColor` in `fromTheme` has already been updated from `colorScheme.outline.withValues(alpha: 0.2)` to `colorScheme.outlineVariant` (M3 alignment).
  - _Leverage: Existing MCalThemeData property patterns, fromTheme factory._
  - _Requirements: 1.1, 10.1, 11.2_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter theme developer. Task: Add eventTileLightContrastColor, eventTileDarkContrastColor (Color?), and hoverEventBackgroundColor (Color?) to MCalThemeData. hoverEventBackgroundColor is being moved from both sub-themes to the shared parent (Req 11.2). Update constructor (all three default to null per Req 1.1), fromTheme (derive from colorScheme: light=Colors.white, dark=colorScheme.onSurface, hover=colorScheme.primaryContainer.withValues(alpha: 0.8)), copyWith, lerp, ==, hashCode. Add dartdoc referencing "master defaults." Restrictions: Do not change any existing property semantics. _Leverage: mcal_theme.dart existing property patterns. _Requirements: 1.1, 10.1, 11.2. Success: Compiles, analyzer clean, existing tests pass. Before implementing: set this task to in-progress [-] in tasks.md. After completing: run log-implementation with artifacts, then set task to complete [x]._

- [x] 2. Add new properties and remove deduplicated properties from MCalDayThemeData
  - File: lib/src/styles/mcal_day_theme_data.dart
  - **Remove 3 properties** (Req 11): `hoverEventBackgroundColor` (moved to shared `MCalThemeData`), `timedEventBorderRadius` (unified with shared `eventTileCornerRadius`), `weekNumberTextColor` (replaced by shared `weekNumberTextStyle`). Remove from field declarations, constructor, `defaults()` factory, `copyWith`, `lerp`, `==`, `hashCode`.
  - **Add 14 new properties**: dropTargetTileBackgroundColor, dropTargetTileInvalidBackgroundColor, dropTargetTileCornerRadius, dropTargetTileBorderColor, dropTargetTileBorderWidth, dropTargetOverlayValidColor, dropTargetOverlayInvalidColor, dropTargetOverlayBorderWidth, dropTargetOverlayBorderColor, disabledTimeSlotColor, resizeHandleColor, keyboardFocusBorderColor, focusedSlotBorderColor, focusedSlotBorderWidth. Also populate the existing `focusedSlotBackgroundColor` in `defaults()` (currently missing; master default: `colorScheme.primary.withValues(alpha: 0.08)`). Update constructor, `defaults()` factory (derive all from colorScheme per M3 roles — notably `dropTargetTileInvalidBackgroundColor` uses `colorScheme.errorContainer` per M3's less-emphasized error role; `disabledTimeSlotColor` uses `colorScheme.onSurface.withValues(alpha: 0.12)` per M3 disabled container pattern), `copyWith`, `lerp`, `==`, `hashCode`. Note: existing `outline.withValues(alpha: ...)` defaults in `defaults()` have already been updated to `outlineVariant`.
  - _Leverage: Existing MCalDayThemeData property patterns, MCalMonthThemeData dropTargetTile* for naming._
  - _Requirements: 3.1, 4.1, 9.1, 11.2, 11.3, 11.4_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter theme developer. Task: (A) Remove 3 deduplicated properties from MCalDayThemeData: hoverEventBackgroundColor (moved to MCalThemeData per Req 11.2), timedEventBorderRadius (unified with shared eventTileCornerRadius per Req 11.3), weekNumberTextColor (replaced by shared weekNumberTextStyle per Req 11.4). Remove from field, constructor, defaults(), copyWith, lerp, ==, hashCode. (B) Add 14 new properties (5 dropTargetTile*, 4 dropTargetOverlay*, disabledTimeSlotColor, resizeHandleColor, keyboardFocusBorderColor, focusedSlotBorderColor, focusedSlotBorderWidth). Update constructor, defaults() factory deriving from colorScheme (see design doc Component 3 table for exact defaults), copyWith, lerp, ==, hashCode. Restrictions: Follow existing property patterns exactly. _Leverage: mcal_day_theme_data.dart, mcal_month_theme_data.dart dropTargetTile* for naming reference. _Requirements: 3.1, 4.1, 9.1, 11.2, 11.3, 11.4. Success: Compiles, analyzer clean, existing tests pass. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 3. Remove deduplicated properties and add new properties to MCalMonthThemeData
  - File: lib/src/styles/mcal_month_theme_data.dart
  - **Pre-applied removals:** `cellBorderColor`, `navigatorBackgroundColor`, and `navigatorTextStyle` have already been removed from `MCalMonthThemeData`. No further action needed for these three.
  - **Remove 10 additional duplicated properties** (Req 11): `cellBackgroundColor`, `allDayEventBackgroundColor`, `allDayEventTextStyle`, `allDayEventBorderColor`, `allDayEventBorderWidth`, `weekNumberTextStyle`, `weekNumberBackgroundColor`, `eventTileCornerRadius`, `eventTileHorizontalSpacing`, `hoverEventBackgroundColor` — all duplicate identically-named properties already on `MCalThemeData`. Remove from field declarations, constructor, `defaults()` factory, `copyWith`, `lerp`, `==`, `hashCode`.
  - **Add 5 new properties**: defaultRegionColor, resizeHandleColor, overlayScrimColor, errorIconColor, overflowIndicatorTextStyle. Update constructor, `defaults()` factory (derive from colorScheme/textTheme per M3 roles — notably `defaultRegionColor` uses `colorScheme.outlineVariant`; also replace Colors.green → colorScheme.tertiary for dropTargetCellValidColor and Colors.red → colorScheme.error for dropTargetCellInvalidColor), `copyWith`, `lerp`, `==`, `hashCode`.
  - _Leverage: Existing MCalMonthThemeData property patterns, defaults() factory._
  - _Requirements: 9.2, 9.4, 9.5, 11.1, 11.2, 11.7_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter theme developer. Task: (A) Remove 10 deduplicated properties from MCalMonthThemeData (see Req 11.1, 11.2): cellBackgroundColor, allDayEventBackgroundColor, allDayEventTextStyle, allDayEventBorderColor, allDayEventBorderWidth, weekNumberTextStyle, weekNumberBackgroundColor, eventTileCornerRadius, eventTileHorizontalSpacing, hoverEventBackgroundColor. These all duplicate properties on MCalThemeData. Remove from field, constructor, defaults(), copyWith, lerp, ==, hashCode. (B) Add 5 new properties (defaultRegionColor, resizeHandleColor, overlayScrimColor, errorIconColor, overflowIndicatorTextStyle). Update constructor, defaults() factory (derive per design doc Component 4 table). Also fix existing defaults: dropTargetCellValidColor Colors.green → colorScheme.tertiary.withValues(alpha: 0.3), dropTargetCellInvalidColor Colors.red → colorScheme.error.withValues(alpha: 0.3). Update copyWith, lerp, ==, hashCode. Restrictions: Follow existing property patterns. _Leverage: mcal_month_theme_data.dart. _Requirements: 9.2, 9.5, 11.1, 11.2, 11.7. Success: Compiles, analyzer clean, no Colors.* in defaults(). Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 4. Modify MCalTheme.of() and fix lerp helpers
  - File: lib/src/styles/mcal_theme.dart
  - Remove `_fillNullSubThemes` static method. Change `of(context)` step 1 and 2 to return data as-is (no _fillNullSubThemes call). Change step 3 fallback from `MCalThemeData.fromTheme(themeData)` to `const MCalThemeData()`. Fix `_lerpMonthTheme`: both null → null, one null → return non-null side, remove `MCalMonthThemeData.defaults(ThemeData.light())`. Fix `_lerpDayTheme` the same way. Update dartdoc on `of()` to describe the new fallback chain and reference "master defaults."
  - _Leverage: Existing MCalTheme.of() implementation, design doc Component 1._
  - _Requirements: 2.2, 2.7, 2.8, 6.1, 6.3_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter InheritedWidget developer. Task: (1) Delete _fillNullSubThemes. (2) In of(context): remove _fillNullSubThemes calls; change step 3 to return const MCalThemeData(). (3) Fix _lerpMonthTheme: both null → null; one null → non-null; remove ThemeData.light(). (4) Fix _lerpDayTheme same way. (5) Update of() dartdoc. Restrictions: Do not change maybeOf() or updateShouldNotify(). Note: this will cause both test failures and widget-level NPEs (widgets that use `theme.monthTheme!.*` or `theme.dayTheme!.*` will now get nulls). Widget code is fixed in tasks 7–14; tests are fixed in task 15. Between task 4 and task 15, flutter test and flutter analyze may report errors — this is expected. _Leverage: mcal_theme.dart, design doc Component 1. _Requirements: 2.2, 2.7, 2.8, 6.1, 6.3. Success: of() returns consumer theme as-is; lerp handles nulls correctly. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

## Phase 2: Cascade utility

- [x] 5. Create shared cascade utility
  - File: lib/src/utils/theme_cascade_utils.dart (new file)
  - Implement `resolveEventTileColor()`, `resolveDropTargetTileColor()`, and `resolveContrastColor()` per the design doc Component 5. The cascade respects `ignoreEventColors`: false → event first; true → theme first. Both modes always include event.color as a fallback. `resolveDropTargetTileColor` accepts an optional `allDayThemeColor` and passes it through to `resolveEventTileColor` so all-day/multi-day drop targets cascade through `allDayEventBackgroundColor`.
  - _Leverage: Design doc Component 5 code listing._
  - _Requirements: 7.1, 7.3, 7.4, 10.2_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Dart utility developer. Task: Create lib/src/utils/theme_cascade_utils.dart with three functions: resolveEventTileColor (ignoreEventColors false: eventColor → allDayThemeColor → themeColor → defaultColor; true: allDayThemeColor → themeColor → eventColor → defaultColor), resolveDropTargetTileColor (dropTargetThemeColor first, then delegates to resolveEventTileColor; accepts optional allDayThemeColor to pass through), resolveContrastColor (luminance-based, uses lightContrastColor/darkContrastColor). Follow exact signatures and dartdoc from design doc Component 5. Restrictions: No Colors.* imports; no Theme.of(context) — this is a pure utility. _Leverage: Design doc Component 5. _Requirements: 7.1, 7.3, 7.4, 10.2. Success: Compiles, pure functions, no side effects. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 6. Add cascade utility tests
  - File: test/utils/theme_cascade_utils_test.dart (new file)
  - Unit tests for all three functions. Test resolveEventTileColor with all combinations: ignoreEventColors true/false × themeColor null/non-null × eventColor null/non-null × allDayThemeColor null/non-null. Test resolveDropTargetTileColor: dropTargetThemeColor takes precedence; allDayThemeColor passes through correctly. Test resolveContrastColor: dark background → light contrast, light background → dark contrast.
  - _Leverage: Existing test patterns in test/._
  - _Requirements: 7.1, NFR Reliability_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Test developer. Task: Create test/utils/theme_cascade_utils_test.dart. Test resolveEventTileColor: (1) ignoreEventColors false, both non-null → eventColor wins; (2) true, both non-null → themeColor wins; (3) true, theme null → eventColor fallback; (4) false, event null → themeColor fallback; (5) all null → defaultColor; (6) allDayThemeColor precedence. Test resolveDropTargetTileColor: dropTargetThemeColor precedence. Test resolveContrastColor: luminance threshold. Restrictions: Use flutter_test. _Leverage: test/ patterns. _Requirements: 7.1, NFR Reliability. Success: All tests pass. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

## Phase 3: Day View widget updates

- [x] 7. Update Day View drop target tile (mcal_day_view.dart)
  - File: lib/src/widgets/mcal_day_view.dart
  - In `_buildDropTargetPreviewLayer`: obtain master defaults via `MCalThemeData.fromTheme(Theme.of(context))`. Replace inline cascade with `resolveDropTargetTileColor()` using `theme.dayTheme?.dropTargetTileBackgroundColor`. When the dragged event is all-day, pass `allDayThemeColor: theme.allDayEventBackgroundColor` so the cascade includes the all-day theme color. Replace hardcoded `Colors.red` invalid color with `theme.dayTheme?.dropTargetTileInvalidBackgroundColor ?? defaults.dayTheme!.dropTargetTileInvalidBackgroundColor`. Use `theme.dayTheme?.dropTargetTileBorderColor/Width ?? defaults.dayTheme!.*` for border. For corner radius, use the three-step cascade per Req 3.3: `theme.dayTheme?.dropTargetTileCornerRadius ?? theme.eventTileCornerRadius ?? defaults.eventTileCornerRadius!`. Remove all `Colors.blue` / `Colors.red` from this method.
  - _Leverage: theme_cascade_utils.dart resolveDropTargetTileColor, design doc Component 7._
  - _Requirements: 2.4, 3.2, 3.3, 8.2, 8.4_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: In mcal_day_view.dart _buildDropTargetPreviewLayer, obtain defaults via MCalThemeData.fromTheme(Theme.of(context)). Replace inline color cascade with resolveDropTargetTileColor(dropTargetThemeColor: theme.dayTheme?.dropTargetTileBackgroundColor, themeColor: theme.eventTileBackgroundColor, allDayThemeColor: (if dragged event is all-day) theme.allDayEventBackgroundColor, eventColor: event.color, ignoreEventColors: theme.ignoreEventColors, defaultColor: defaults.eventTileBackgroundColor!). For corner radius, use three-step cascade per Req 3.3: theme.dayTheme?.dropTargetTileCornerRadius ?? theme.eventTileCornerRadius ?? defaults.eventTileCornerRadius!. Replace all hardcoded Colors.blue/red with theme/defaults properties (dropTargetTileInvalidBackgroundColor, dropTargetTileBorderColor, dropTargetTileBorderWidth). Import theme_cascade_utils.dart. Restrictions: No Colors.* in this method. _Leverage: theme_cascade_utils.dart, mcal_day_theme_data.dart. _Requirements: 2.4, 3.2, 3.3, 8.2, 8.4. Success: No Colors.* in method; cascade uses utility; corner radius cascades through shared eventTileCornerRadius. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 8. Update Day View drop target overlay (mcal_day_view.dart)
  - File: lib/src/widgets/mcal_day_view.dart
  - In `_buildDropTargetOverlayLayer`: obtain master defaults. Replace `Colors.blue.withValues(alpha: 0.2)` / `Colors.red.withValues(alpha: 0.2)` with `theme.dayTheme?.dropTargetOverlayValidColor ?? defaults.dayTheme!.dropTargetOverlayValidColor` (and invalid). Replace hardcoded border width `3` with `theme.dayTheme?.dropTargetOverlayBorderWidth ?? defaults.dayTheme!.dropTargetOverlayBorderWidth`. Replace border color with `theme.dayTheme?.dropTargetOverlayBorderColor ?? defaults.dayTheme!.dropTargetOverlayBorderColor`.
  - _Leverage: Design doc Component 3 table for defaults._
  - _Requirements: 4.1, 4.2_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: In mcal_day_view.dart _buildDropTargetOverlayLayer, obtain defaults. Replace Colors.blue/red overlay colors with theme.dayTheme?.dropTargetOverlayValidColor/InvalidColor ?? defaults. Replace hardcoded border width 3 and color with theme.dayTheme?.dropTargetOverlayBorderWidth/Color ?? defaults. Restrictions: No Colors.* in this method; no direct Theme.of(context).colorScheme. _Requirements: 4.1, 4.2. Success: Overlay fully themed. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 9. Update Day View focused slot and keyboard focus (mcal_day_view.dart)
  - File: lib/src/widgets/mcal_day_view.dart
  - In `_buildFocusedSlotIndicator` and empty schedule focus: replace `Theme.of(context).colorScheme.primary` with `theme.dayTheme?.focusedSlotBorderColor ?? defaults.dayTheme!.focusedSlotBorderColor` and `theme.dayTheme?.focusedSlotBorderWidth ?? defaults.dayTheme!.focusedSlotBorderWidth`. The `focusedSlotBackgroundColor` fallback also goes through defaults.
  - _Leverage: Design doc, Req 9.1._
  - _Requirements: 9.1, 9.4, NFR No Direct colorScheme_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: In mcal_day_view.dart, update _buildFocusedSlotIndicator and the empty schedule focus indicator. Replace all Theme.of(context).colorScheme.primary with theme.dayTheme?.focusedSlotBorderColor ?? defaults.dayTheme!.focusedSlotBorderColor. Replace hardcoded width 3 with theme.dayTheme?.focusedSlotBorderWidth ?? defaults.dayTheme!.focusedSlotBorderWidth. Replace focusedSlotBackgroundColor fallback colorScheme.primary.withValues(alpha: 0.08) with defaults.dayTheme!.focusedSlotBackgroundColor. Restrictions: No Theme.of(context).colorScheme in widget code. _Requirements: 9.1, 9.4, NFR. Success: No direct colorScheme access. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 10. Update Day View sub-widgets (7 files)
  - Files: time_grid_events_layer.dart, all_day_events_section.dart, gridlines_layer.dart, time_regions_layer.dart, time_legend_column.dart, current_time_indicator.dart, day_navigator.dart
  - In each file: obtain master defaults. Replace all Colors.* fallbacks and direct colorScheme access with `theme.* ?? defaults.*` pattern. In time_grid_events_layer: replace `_getContrastColor` with `resolveContrastColor()` from cascade utility, using `theme.eventTileLightContrastColor ?? defaults.eventTileLightContrastColor!` and dark variant. Replace tile color cascade with `resolveEventTileColor()`. **Replace `theme.dayTheme?.timedEventBorderRadius ?? theme.eventTileCornerRadius ?? 3.0` with `theme.eventTileCornerRadius ?? defaults.eventTileCornerRadius!`** (Req 11.3 — `timedEventBorderRadius` has been removed; shared `eventTileCornerRadius` is the single property). Ensure text style color precedence per Req 10.3: when `ignoreEventColors` is true and the consumer's `eventTileTextStyle` has a color, that color takes precedence over the luminance-based contrast color. In all_day_events_section: same for tile cascade, keyboard focus border (use keyboardFocusBorderColor), and contrast/text style precedence. In time_legend_column: replace colorScheme.outline fallback with defaults. In all files: replace keyboard focus `colorScheme.primary` with `theme.dayTheme?.keyboardFocusBorderColor ?? defaults.dayTheme!.keyboardFocusBorderColor`.
  - _Leverage: theme_cascade_utils.dart, design doc Component 7 tables._
  - _Requirements: 2.3, 7.2, 8.1, 8.4, 9.1, 10.2, 10.3, 11.3, NFR_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: Update 7 Day View sub-widget files. In each: (1) obtain defaults = MCalThemeData.fromTheme(Theme.of(context)). (2) Replace every Colors.* fallback with theme.property ?? defaults.property (or defaults.dayTheme!.property for day-specific). (3) In time_grid_events_layer: replace _getContrastColor with resolveContrastColor from theme_cascade_utils; replace tile color cascade with resolveEventTileColor; replace timedEventBorderRadius cascade with theme.eventTileCornerRadius ?? defaults.eventTileCornerRadius! (timedEventBorderRadius is removed per Req 11.3); replace keyboard focus colorScheme.primary with theme.dayTheme?.keyboardFocusBorderColor ?? defaults. (4) In all_day_events_section: same cascade + keyboard focus + contrast changes. (5) In time_legend_column: replace colorScheme.outline fallback with defaults.dayTheme!.timeLegendTickColor. (6) In gridlines_layer, time_regions_layer, current_time_indicator: replace Colors.grey/red/black54 with defaults. (7) In day_navigator: obtain defaults; replace null fallbacks for navigatorBackgroundColor and navigatorTextStyle with theme.navigatorBackgroundColor ?? defaults.navigatorBackgroundColor and theme.navigatorTextStyle ?? defaults.navigatorTextStyle. Restrictions: No Colors.* or colorScheme in widget code. Import theme_cascade_utils.dart. _Requirements: 2.3, 7.2, 8.1, 8.4, 9.1, 10.2, 11.3, NFR. Success: Zero Colors.* in these 7 files. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 11. Update Day View remaining sub-widgets (3 files)
  - Files: day_header.dart, disabled_time_slots_layer.dart, shared/time_resize_handle.dart
  - In day_header: obtain defaults; replace Colors.grey, Colors.black54, Colors.grey[600], Colors.black87 with defaults. **Replace `theme.dayTheme?.weekNumberTextColor ?? Colors.black54` with `theme.weekNumberTextStyle ?? defaults.weekNumberTextStyle!`** (Req 11.4 — `weekNumberTextColor` has been removed; shared `weekNumberTextStyle` from the parent is the single property). In disabled_time_slots_layer: add `Color? disabledTimeSlotColor` parameter; parent passes resolved color. In time_resize_handle: add `Color? resizeHandleColor` parameter; parent passes resolved color. Update all parent call sites to pass resolved colors from theme/defaults.
  - _Leverage: Design doc Component 7 widgets needing parameter additions._
  - _Requirements: 9.1, 9.5, 9.6, 11.4, 11.5_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: (1) day_header.dart: obtain defaults, replace all Colors.grey/black54/grey[600]/black87 with theme ?? defaults. Replace weekNumberTextColor cascade (theme.dayTheme?.weekNumberTextColor ?? Colors.black54) with theme.weekNumberTextStyle ?? defaults.weekNumberTextStyle! — weekNumberTextColor is removed per Req 11.4; use the shared weekNumberTextStyle from MCalThemeData parent which includes the color. (2) disabled_time_slots_layer.dart: add Color? disabledTimeSlotColor parameter; use it instead of Colors.grey.withValues(alpha: 0.3); update parent call site to pass theme.dayTheme?.disabledTimeSlotColor ?? defaults.dayTheme!.disabledTimeSlotColor. (3) time_resize_handle.dart: add Color? resizeHandleColor parameter; use it; update parent to pass resolved color. Restrictions: Keep widgets simple — pass resolved colors, not full theme. _Requirements: 9.1, 9.5, 9.6, 11.4, 11.5. Success: No Colors.* in these files; parents pass resolved colors. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

## Phase 4: Month View widget updates

- [x] 12. Update Month View drop target tile (month_page_widget.dart)
  - File: lib/src/widgets/month_subwidgets/month_page_widget.dart
  - In `_buildDefaultDropTargetTile`: obtain master defaults. Replace inline cascade with `resolveDropTargetTileColor()`, passing `allDayThemeColor: theme.allDayEventBackgroundColor` when the dragged event is multi-day/all-day so the full cascade is respected. Replace all Colors.blue/red/white/green fallbacks with theme/defaults properties. For `DropTargetHighlightPainter` call: replace Colors.green/red fallbacks with `theme.monthTheme?.dropTargetCellValidColor ?? defaults.monthTheme!.dropTargetCellValidColor` (and invalid). For the resize handle in drop target: pass resolved `resizeHandleColor`.
  - _Leverage: theme_cascade_utils.dart, design doc Component 7._
  - _Requirements: 2.4, 8.2, 8.4, NFR DropTargetHighlightPainter_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: In month_page_widget.dart _buildDefaultDropTargetTile: (1) obtain defaults. (2) Replace tile color cascade with resolveDropTargetTileColor, passing allDayThemeColor: theme.allDayEventBackgroundColor when the dragged event is multi-day/all-day. (3) Replace all Colors.blue/red with theme/defaults for invalid color, border color, fill. (4) For DropTargetHighlightPainter: pass theme.monthTheme?.dropTargetCellValidColor ?? defaults.monthTheme!.dropTargetCellValidColor (and invalid). (5) For resize handle: pass resolved resizeHandleColor. Restrictions: No Colors.* in this method. _Requirements: 2.4, 8.2, 8.4. Success: No Colors.* in drop target code. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 13. Update DropTargetHighlightPainter
  - Files: lib/src/widgets/month_subwidgets/drop_target_highlight_painter.dart, and all call sites (month_page_widget.dart, test files)
  - Make `validColor` and `invalidColor` required parameters (remove default hex literal values `Color(0x4000FF00)` and `Color(0x40FF0000)`). Search all call sites — both in `month_page_widget.dart` (updated in task 12) and in test files — and ensure every instantiation of `DropTargetHighlightPainter` passes explicit `validColor` and `invalidColor`. Test call sites should use theme-derived or hardcoded test colors.
  - _Leverage: NFR DropTargetHighlightPainter._
  - _Requirements: NFR DropTargetHighlightPainter_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Dart API developer. Task: In drop_target_highlight_painter.dart, make validColor and invalidColor required constructor parameters. Remove their default values (Color(0x4000FF00) and Color(0x40FF0000)). Then search the entire codebase (lib/ and test/) for DropTargetHighlightPainter instantiations and ensure every one passes explicit validColor and invalidColor. Restrictions: All callers — including tests — must compile. _Requirements: NFR DropTargetHighlightPainter. Success: No default color literals; all callers compile. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 14. Update Month View sub-widgets (8 files)
  - Files: week_row_widget.dart, day_cell_widget.dart, weekday_header_row_widget.dart, week_number_cell.dart, month_overlays.dart, mcal_month_multi_day_tile.dart, shared_subwidgets/month_resize_handle.dart, month_navigator_widget.dart
  - In each: obtain master defaults. Replace all Colors.* and colorScheme with theme/defaults. **Property deduplication (Req 11)**: The following properties have been removed from `MCalMonthThemeData` and must now be accessed via the shared parent `MCalThemeData`: `cellBorderColor` (already applied), `navigatorBackgroundColor` (already applied), `navigatorTextStyle` (already applied), `cellBackgroundColor`, `allDayEventBackgroundColor`, `allDayEventTextStyle`, `allDayEventBorderColor`, `allDayEventBorderWidth`, `weekNumberTextStyle`, `weekNumberBackgroundColor`, `eventTileCornerRadius`, `eventTileHorizontalSpacing`, `hoverEventBackgroundColor`. Any widget code still using `theme.monthTheme?.{property}` for these properties must be changed to `theme.{property} ?? defaults.{property}!`. In week_row_widget: (1) replace tile color cascade with resolveEventTileColor; (2) keyboard selection border uses resolveContrastColor with contrast theme properties; (3) event tile text style falls through to defaults; ensure text style color precedence per Req 10.3; (4) date label styling uses defaults; (5) overflow indicator: replace the `leadingDatesTextStyle` fallback hack with `theme.monthTheme?.overflowIndicatorTextStyle ?? defaults.monthTheme!.overflowIndicatorTextStyle`. In day_cell_widget: replace Colors.grey region fallback with theme.monthTheme?.defaultRegionColor ?? defaults; **replace `theme.monthTheme?.cellBackgroundColor` cascade with `theme.cellBackgroundColor ?? defaults.cellBackgroundColor`**. In weekday_header_row_widget and week_number_cell: **replace `theme.monthTheme?.weekNumberBackgroundColor` with `theme.weekNumberBackgroundColor ?? defaults.weekNumberBackgroundColor`; replace `theme.monthTheme?.weekNumberTextStyle` with `theme.weekNumberTextStyle ?? defaults.weekNumberTextStyle!`**. In month_overlays: replace scrim Colors.black with overlayScrimColor, replace colorScheme.error with errorIconColor. In mcal_month_multi_day_tile: use resolveEventTileColor with allDayThemeColor, respecting ignoreEventColors (Req 5). In month_resize_handle: add Color? resizeHandleColor parameter; parent passes resolved color.
  - _Leverage: theme_cascade_utils.dart, design doc Component 7 tables._
  - _Requirements: 2.3, 5.1, 5.2, 7.2, 8.1, 8.4, 9.2, 9.3, 10.2, 10.3, 11.1, 11.2, 11.6_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter widget developer. Task: Update 8 Month View files. In each: obtain defaults = MCalThemeData.fromTheme(Theme.of(context)). **Critical deduplication step (Req 11)**: The following properties have been removed from MCalMonthThemeData — any `theme.monthTheme?.{prop}` access must be changed to `theme.{prop} ?? defaults.{prop}!`: cellBackgroundColor, allDayEventBackgroundColor, allDayEventTextStyle, allDayEventBorderColor, allDayEventBorderWidth, weekNumberTextStyle, weekNumberBackgroundColor, eventTileCornerRadius, eventTileHorizontalSpacing, hoverEventBackgroundColor. (1) week_row_widget: replace tile cascade with resolveEventTileColor; keyboard border with resolveContrastColor; text style/date label fallbacks via defaults; overflow indicator via theme.monthTheme?.overflowIndicatorTextStyle ?? defaults; Req 10.3 text style color precedence. (2) day_cell_widget: region color via monthTheme?.defaultRegionColor ?? defaults; cellBackgroundColor and cellBorderColor via shared theme.* ?? defaults.* (not monthTheme). (3) weekday_header_row_widget, week_number_cell: cellBorderColor, weekNumberBackgroundColor, weekNumberTextStyle all via shared theme.* ?? defaults.*. (4) month_overlays: scrim via monthTheme?.overlayScrimColor ?? defaults; error icon via monthTheme?.errorIconColor ?? defaults. (5) mcal_month_multi_day_tile: resolveEventTileColor with allDayThemeColor, respecting ignoreEventColors. (6) month_resize_handle: add Color? resizeHandleColor param. (7) month_navigator_widget: navigatorBackgroundColor/TextStyle via shared theme.* ?? defaults.*. Restrictions: No Colors.* or colorScheme in widget code; no monthTheme access for deduplicated properties. _Requirements: 2.3, 5.1, 5.2, 7.2, 8.1, 8.4, 9.2, 9.3, 10.2, 10.3, 11.1, 11.2, 11.6. Success: Zero Colors.* in these files; zero monthTheme access for deduplicated properties. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

## Phase 5: Tests and documentation

- [x] 15. Fix existing tests broken by MCalTheme.of() changes
  - Files: test/ (various existing test files)
  - After task 4, `MCalTheme.of()` no longer fills null sub-themes or falls back to `fromTheme()`. Existing tests that relied on `theme.monthTheme` or `theme.dayTheme` being auto-populated will now get nulls. Fix by wrapping test widgets in `MCalTheme(data: MCalThemeData.fromTheme(theme))` where a fully populated theme is expected, or by updating assertions to match the new null-returning behavior. **Tests that instantiate `MCalMonthThemeData` or `MCalDayThemeData` with removed properties** (e.g. `cellBackgroundColor`, `hoverEventBackgroundColor`, `timedEventBorderRadius`, `weekNumberTextColor`) will fail to compile after tasks 2–3. Migrate these to use `MCalThemeData` for parent-level properties, or remove the removed properties from constructor calls.
  - _Leverage: Existing test infrastructure._
  - _Requirements: 2.2, 6.1, NFR Reliability_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Test developer. Task: Run flutter test and identify all failures caused by (1) MCalTheme.of() returning MCalThemeData() instead of fromTheme() (2) null monthTheme/dayTheme sub-themes (3) Colors.* removal in widget code (4) tests that instantiate MCalMonthThemeData or MCalDayThemeData with removed properties (cellBackgroundColor, hoverEventBackgroundColor, timedEventBorderRadius, weekNumberTextColor, etc.) — these will fail to compile; migrate to MCalThemeData for parent-level properties or remove the removed properties from constructor calls. Fix each by: wrapping in MCalTheme(data: MCalThemeData.fromTheme(theme)) if the test needs a full theme, or updating assertions for new defaults. Do NOT re-introduce _fillNullSubThemes or Colors.* fallbacks. Restrictions: All existing tests must pass. _Requirements: 2.2, 6.1, NFR Reliability. Success: flutter test passes 100%. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 16. Add new theme cascade tests
  - Files: test/styles/mcal_theme_test.dart, test/styles/mcal_day_theme_data_test.dart, test/styles/mcal_month_theme_data_test.dart (new or existing)
  - Test MCalTheme.of() returns consumer theme as-is (nulls preserved, no _fillNullSubThemes). Test lerp: both null → null, one null → non-null. Test new properties in copyWith, lerp, ==, hashCode for all three theme classes. Test defaults() factories populate all new properties with non-null values. **Test property consolidation (Req 11)**: (a) Verify `hoverEventBackgroundColor` exists on `MCalThemeData` (copyWith, lerp, ==, hashCode, fromTheme). (b) Verify `hoverEventBackgroundColor`, `timedEventBorderRadius`, `weekNumberTextColor` do NOT exist on `MCalDayThemeData` (removed). (c) Verify `cellBackgroundColor`, `allDayEventBackgroundColor`, `allDayEventTextStyle`, `allDayEventBorderColor`, `allDayEventBorderWidth`, `weekNumberTextStyle`, `weekNumberBackgroundColor`, `eventTileCornerRadius`, `eventTileHorizontalSpacing`, `hoverEventBackgroundColor` do NOT exist on `MCalMonthThemeData` (removed). (d) Verify `MCalMonthThemeData.defaults()` does not populate any of the removed properties. (e) Verify `MCalDayThemeData.defaults()` does not populate any of the removed properties.
  - _Leverage: Existing theme test patterns._
  - _Requirements: 1.1, 2.2, 2.7, 2.8, 3.1, 4.1, 9.1, 9.2, 10.1, 11.1, 11.2, 11.3, 11.4_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Test developer. Task: Add theme cascade tests. (1) MCalTheme.of: returns consumer theme as-is with nulls; returns MCalThemeData() when no ancestor. (2) Lerp: both null → null; one null → non-null; no ThemeData.light(). (3) MCalThemeData: new properties (eventTileLightContrastColor, eventTileDarkContrastColor, hoverEventBackgroundColor) in copyWith/lerp/==/hashCode; fromTheme populates them. (4) MCalDayThemeData: 14 new properties in copyWith/lerp/==/hashCode; defaults() populates all; verify hoverEventBackgroundColor, timedEventBorderRadius, weekNumberTextColor are absent. (5) MCalMonthThemeData: 5 new properties; defaults() replaces Colors.green/red with colorScheme; verify 10 removed properties (cellBackgroundColor, allDayEvent*, weekNumber*, eventTileCornerRadius, eventTileHorizontalSpacing, hoverEventBackgroundColor) are absent. Restrictions: Use flutter_test. _Requirements: 1.1, 2.2, 2.7, 2.8, 3.1, 4.1, 9.1, 9.2, 10.1, 11.1, 11.2, 11.3, 11.4. Success: All new tests pass. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 17. Add widget-level cascade integration tests
  - Files: test/widgets/ (new test files or additions to existing)
  - Test that each tile type uses the correct cascade order. Test ignoreEventColors: false → event color wins; true → theme wins. Test multi-day tile respects ignoreEventColors. Test drop target tiles use dropTargetTile* properties (including allDayThemeColor pass-through for multi-day drop targets). Test Day View overlay uses overlay theme properties. Test keyboard focus borders use theme properties. Test Req 10.3: when ignoreEventColors is true and eventTileTextStyle has a color, the text style color takes precedence over the luminance-based contrast color. Test no Colors.* appear in rendered widget trees.
  - _Leverage: Existing widget test patterns, flutter_test._
  - _Requirements: 2.3, 2.5, 5.1, 5.2, 8.1, 8.3, 10.3_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Test developer. Task: Add widget-level cascade tests. (1) Timed event tile: ignoreEventColors false with event.color → renders event color; true → renders theme color. (2) All-day tile: allDayEventBackgroundColor cascades correctly. (3) Multi-day tile: ignoreEventColors respected. (4) Drop target tile (Day + Month): dropTargetTileBackgroundColor takes precedence; allDayThemeColor passes through for multi-day events. (5) Day View overlay: dropTargetOverlayValidColor used. (6) Keyboard focus border: keyboardFocusBorderColor used. (7) Contrast color: eventTileLightContrastColor/DarkContrastColor used for text. (8) Text style color precedence (Req 10.3): ignoreEventColors true + eventTileTextStyle with color → text style color wins over contrast. Restrictions: Use flutter_test pumpWidget with MCalTheme wrapping. _Requirements: 2.3, 2.5, 5.1, 5.2, 8.1, 8.3, 10.3. Success: All tests pass, cover both ignoreEventColors modes. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 18. Update dartdoc and exports
  - Files: lib/src/styles/mcal_theme.dart, lib/src/styles/mcal_day_theme_data.dart, lib/src/styles/mcal_month_theme_data.dart, lib/multi_calendar.dart
  - Add comprehensive dartdoc to all new properties referencing "master defaults" and cascade behavior. Update MCalThemeData class-level dartdoc to describe the cascade model. Update MCalTheme.of() dartdoc. Ensure lib/multi_calendar.dart exports are correct (theme_cascade_utils.dart should NOT be exported — it's internal).
  - _Leverage: Existing dartdoc style._
  - _Requirements: NFR Documentation_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Technical writer. Task: Add dartdoc to all new properties on MCalThemeData, MCalDayThemeData, MCalMonthThemeData. Reference "master defaults" concept. Update MCalThemeData class dartdoc to explain cascade: ignoreEventColors false → event → theme → defaults; true → theme → event → defaults. Update MCalTheme.of() dartdoc. Verify theme_cascade_utils.dart is NOT exported from multi_calendar.dart. Restrictions: Match existing dartdoc style. _Requirements: NFR Documentation. Success: dart doc generates clean docs; no missing doc warnings on public API. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 19. Update example app
  - Files: example/lib/views/month_view/*, example/lib/views/day_view/*, example/lib/shared/utils/theme_presets.dart, example/lib/views/day_view/day_view_theme_settings.dart
  - Ensure example app compiles and runs with the refactored theme system. **Property deduplication changes (Req 11)** — the following example app updates are required:
    - **`cellBorderColor`, `navigatorBackgroundColor`, `navigatorTextStyle`**: Already moved from `MCalMonthThemeData` to `MCalThemeData` in example code.
    - **`cellBackgroundColor` on `monthTheme`**: Used in `month_colorful_style.dart`, `month_modern_style.dart`, `month_accessibility_tab.dart` — move to `MCalThemeData` constructor.
    - **`hoverEventBackgroundColor` on `monthTheme`**: Used in `month_accessibility_tab.dart`, `month_theme_tab.dart` — move to `MCalThemeData` constructor.
    - **`eventTileCornerRadius` on `monthTheme`**: Used in `month_colorful_style.dart` — remove from `MCalMonthThemeData` (parent already sets it). Also update `month_colorful_style.dart` cascade reads from `theme.monthTheme?.eventTileCornerRadius` to `theme.eventTileCornerRadius`.
    - **`timedEventBorderRadius` on `dayTheme`**: Used in `day_classic_style.dart`, `day_modern_style.dart`, `day_colorful_style.dart`, `day_minimal_style.dart`, `day_theme_tab.dart`, `theme_presets.dart`, `day_view_theme_settings.dart` — rename to `eventTileCornerRadius` and move to `MCalThemeData` constructor (or keep as separate Day-specific config in the example's own `DayViewThemeSettings` helper, mapped to the parent property).
    - **`weekNumberTextColor` on `dayTheme`**: Not used in example app (no changes needed).
    - **`weekNumberTextStyle` / `weekNumberBackgroundColor` on `monthTheme`**: Not used in example (they use parent already). No changes needed.
  - _Leverage: Existing example app structure._
  - _Requirements: 11.1, 11.2, 11.3, NFR Usability_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Flutter app developer. Task: Update the example app for the refactored theme cascade and property consolidation. (1) Move monthTheme.cellBackgroundColor to MCalThemeData in month_colorful_style, month_modern_style, month_accessibility_tab. (2) Move monthTheme.hoverEventBackgroundColor to MCalThemeData in month_accessibility_tab, month_theme_tab. (3) Remove monthTheme.eventTileCornerRadius in month_colorful_style (parent already sets it); update cascade reads from theme.monthTheme?.eventTileCornerRadius to theme.eventTileCornerRadius. (4) Replace dayTheme.timedEventBorderRadius with eventTileCornerRadius on MCalThemeData in day_classic_style, day_modern_style, day_colorful_style, day_minimal_style, theme_presets. (5) In day_theme_tab and day_view_theme_settings: rename timedEventBorderRadius to eventTileCornerRadius; set on MCalThemeData instead of dayTheme. (6) Verify visual appearance is preserved. Restrictions: Do not re-introduce Colors.* in library code. _Requirements: 11.1, 11.2, 11.3, NFR Usability. Success: Example app compiles, runs, looks the same. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._

- [x] 20. Final verification — zero Colors.* audit
  - Files: lib/src/ (all files)
  - Run a grep/search for `Colors.` across all files in lib/src/. Verify zero hits in widget and tile-building code except `Colors.transparent` (exempted by NFR as a "no fill" sentinel). The only other allowed locations are inside `fromTheme()` and `defaults()` factory methods. Fix any remaining violations. Also search for `Theme.of(context).colorScheme` — must only appear in factory methods.
  - _Leverage: Grep/rg tool._
  - _Requirements: NFR Centralized Defaults, NFR No Direct colorScheme_
  - _Prompt: Implement the task for spec theme-cascade-refactor, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Code auditor. Task: Search lib/src/ for Colors. and Theme.of(context).colorScheme usages. Verify the ONLY locations for Colors.* (other than Colors.transparent) are inside fromTheme()/defaults() factories in mcal_theme.dart, mcal_month_theme_data.dart, mcal_day_theme_data.dart. Colors.transparent is permitted anywhere as a "no fill" sentinel per the NFR exemption. Fix any violations found elsewhere. Also verify Theme.of(context).colorScheme only appears in factory methods. Restrictions: Do not remove Colors.* from factory methods; do not remove Colors.transparent from widget code. _Requirements: NFR Centralized Defaults, NFR No Direct colorScheme. Success: Zero non-transparent Colors.* in widget/tile code; zero colorScheme in widget code. Before implementing: set task [-] in tasks.md. After completing: log-implementation then set [x]._
