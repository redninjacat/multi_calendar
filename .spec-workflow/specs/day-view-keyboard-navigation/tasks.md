# Tasks Document: Day View Keyboard Navigation

## Phase 1: Data Model & Infrastructure

- [ ] 1. Create `MCalDayKeyBindings` class
  - File: `lib/src/models/mcal_day_key_bindings.dart` (NEW)
  - Create `MCalDayKeyBindings` immutable class with `const` default constructor. 24 named `List<MCalKeyActivator>` properties across 4 modes: Navigation Mode (8: `enterEventMode`, `home`, `end`, `pageUp`, `pageDown`, `createEvent`, `jumpToAllDay`, `jumpToTimeGrid`), Event Mode (8: `cycleForward`, `cycleBackward`, `activate`, `delete`, `enterMoveMode`, `enterResizeMode`, `exitEventMode`, `convertEventType`), Move Mode (3: `confirmMove`, `cancelMove`, `switchToResize`), Resize Mode (5: `switchToStartEdge`, `switchToEndEdge`, `confirmResize`, `switchToMove`, `cancelResize`). Default values match the Month View where applicable plus Day View-specific additions. Implement `copyWith` for selective overrides. Import `MCalKeyActivator` from `mcal_month_key_bindings.dart`.
  - Purpose: Data model for Day View configurable key bindings
  - _Leverage: `MCalMonthKeyBindings` in `lib/src/models/mcal_month_key_bindings.dart` — follow identical patterns for constructor, field declarations, `copyWith`, `==`, `hashCode`, `toString`. Reuse `MCalKeyActivator` from the same file._
  - _Requirements: 6.1, 6.2, 6.3, 6.6, 6.7, 6.8_
  - _Prompt: Role: Dart Developer specializing in immutable data models | Task: Create `MCalDayKeyBindings` in a new file `lib/src/models/mcal_day_key_bindings.dart` as specified in the design doc `.spec-workflow/specs/day-view-keyboard-navigation/design.md` section "Component 1: MCalDayKeyBindings". Import `MCalKeyActivator` from `package:multi_calendar/src/models/mcal_month_key_bindings.dart` and `LogicalKeyboardKey` from `package:flutter/services.dart`. Create 24 named `List<MCalKeyActivator>` properties with `const` defaults exactly as listed in the design. The 3 Day View-specific additions vs Month View are: `jumpToAllDay` (default: A), `jumpToTimeGrid` (default: T), `convertEventType` (default: X). Implement `copyWith` returning a new instance with overridden properties (nullable parameters, use `??` fallback). Implement `==` comparing all 24 fields, `hashCode` using `Object.hashAll`, and `toString` for debugging. Add comprehensive dartdoc for the class and each property group. Arrow keys are NOT included — they are hardcoded for directional navigation. | Restrictions: Do NOT modify existing files. Do NOT export from `multi_calendar.dart` yet (Task 2). Both class must have `const` constructor. Follow the exact structure of `MCalMonthKeyBindings` for consistency. | Success: `dart analyze lib/src/models/mcal_day_key_bindings.dart` clean, `const MCalDayKeyBindings()` compiles, all 24 properties have correct default values._

- [ ] 2. Export `MCalDayKeyBindings` from `multi_calendar.dart`
  - File: `lib/multi_calendar.dart` (modify existing)
  - Add `export 'src/models/mcal_day_key_bindings.dart';` in alphabetical order with other model exports.
  - Purpose: Make Day View key bindings class part of the public API
  - _Leverage: Existing export pattern in `lib/multi_calendar.dart` — see how `mcal_month_key_bindings.dart` is exported_
  - _Requirements: 6.1 (public API availability), NFR Code Architecture_
  - _Prompt: Role: Dart Developer | Task: Add `export 'src/models/mcal_day_key_bindings.dart';` to `lib/multi_calendar.dart`, in alphabetical order with the other model exports. Look at how `mcal_month_key_bindings.dart` is already exported and follow the same pattern. | Restrictions: Do NOT remove existing exports. Only add one line. | Success: `dart analyze` clean, `MCalDayKeyBindings` importable via `package:multi_calendar/multi_calendar.dart`._

- [ ] 3. Add focused slot theme properties to `MCalDayThemeData`
  - File: `lib/src/styles/mcal_day_theme_data.dart` (modify existing)
  - Add two new optional properties for Navigation Mode's focused slot visual indicator: `focusedSlotBackgroundColor` (`Color?`) and `focusedSlotDecoration` (`BoxDecoration?`). These allow consumers to customize how the focused time slot appears in Navigation Mode. Add them to the constructor, `copyWith`, `lerp`, `==`, and `hashCode`.
  - Purpose: Theme support for focused slot visual indicator
  - _Leverage: Existing theme properties in `MCalDayThemeData` (e.g., `allDayTileWidth`, `allDayTileHeight`) — follow identical pattern for adding new properties_
  - _Requirements: 2.10 (focused slot visually indicated, configurable through MCalDayThemeData)_
  - _Prompt: Role: Flutter Developer | Task: Add two new optional properties to `MCalDayThemeData` in `lib/src/styles/mcal_day_theme_data.dart`: `final Color? focusedSlotBackgroundColor;` and `final BoxDecoration? focusedSlotDecoration;`. The `focusedSlotBackgroundColor` provides a simple background color for the focused time slot. The `focusedSlotDecoration` provides full control via `BoxDecoration` (border, gradient, etc.) and takes precedence over `focusedSlotBackgroundColor` when both are set. Add both to the constructor as optional named parameters, to `copyWith` with nullable parameters and `??` fallback, to `lerp` (lerp the color, null the decoration since BoxDecoration.lerp exists but is complex), to `==` and `hashCode`. Add dartdoc explaining these are used in Navigation Mode to indicate the focused time slot. | Restrictions: Do NOT remove or modify existing properties. Follow the exact pattern used for `allDayTileWidth` and similar recent additions. | Success: `dart analyze` clean, `MCalDayThemeData(focusedSlotBackgroundColor: Colors.blue.withOpacity(0.1))` compiles._

- [ ] 4. Add package-level localization strings for keyboard announcements
  - File: `lib/l10n/app_en.arb` (and `app_ar.arb`, `app_es.arb`, `app_fr.arb`, `app_he.arb`)
  - Add 17 new announcement strings for Day View keyboard navigation to all 5 package-level ARB files. Follow the Month View's `announcement*` naming pattern. Strings: `announcementDayNavigationMode`, `announcementDayEventMode`, `announcementDayEventCycled`, `announcementDayEventSelected`, `announcementDayMoveMode`, `announcementDayResizeMode`, `announcementDayMoveCancelled`, `announcementDayResizeCancelled`, `announcementDayMovingEvent`, `announcementDayResizingEvent`, `announcementDayEventMoved`, `announcementDayEventResized`, `announcementDayAllDaySection`, `announcementDayTimeGrid`, `announcementDayMoveInvalidTarget`, `announcementDayEventDeleted`, `announcementDayEventConversionRequested`. After adding to all ARB files, run `flutter pub get` to regenerate `MCalLocalizations`.
  - Purpose: Localized screen reader announcements for all keyboard mode transitions
  - _Leverage: Existing announcement strings in `lib/l10n/app_en.arb` (e.g., `announcementEventSelected`, `announcementResizeModeEntered`) — follow identical naming and metadata patterns. Check `lib/l10n/mcal_localizations.dart` to see generated accessor pattern._
  - _Requirements: NFR Accessibility (localized announcements in all 5 ARB files), 1.6, 3.8, 3.9_
  - _Prompt: Role: Flutter Developer with i18n expertise | Task: Add 17 new localization strings to all 5 package-level ARB files (`lib/l10n/app_en.arb`, `app_ar.arb`, `app_es.arb`, `app_fr.arb`, `app_he.arb`) as specified in the design doc section "Component 3: Localization additions". For each string, add the key-value pair AND an `@key` metadata entry with `description` and `placeholders` (where applicable). Use ICU message format for parameterized strings — e.g., `"announcementDayEventCycled": "{title}, {position} of {total}"` with `@announcementDayEventCycled: { "placeholders": { "title": {}, "position": {}, "total": {} } }`. Translate all strings to the appropriate language for each ARB file (English, Arabic, Spanish, French, Hebrew). After adding all strings, run `flutter pub get` from the project root to regenerate `MCalLocalizations`. Verify the generated `lib/l10n/mcal_localizations.dart` includes the new accessor methods. | Restrictions: Do NOT modify existing strings. Do NOT modify example app ARB files (those are Task 13). Place new entries after existing announcement strings for consistency. Ensure proper JSON syntax. | Success: `flutter pub get` succeeds, `MCalLocalizations` class has new accessors like `announcementDayNavigationMode`, `announcementDayEventCycled(title, position, total)`, etc._

## Phase 2: API Migration & Legacy Removal

- [ ] 5. Remove legacy shortcuts system, update callback signatures, add new callbacks and parameters
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - File: `lib/multi_calendar.dart` (modify existing)
  - **Remove from `mcal_day_view.dart`:**
    - `MCalDayViewCreateEventIntent` class (line ~36)
    - `MCalDayViewDeleteEventIntent` class (line ~42)
    - `MCalDayViewEditEventIntent` class (line ~48)
    - `MCalDayViewKeyboardMoveIntent` class (line ~54)
    - `MCalDayViewKeyboardResizeIntent` class (line ~60)
    - `keyboardShortcuts` parameter (constructor line ~219, field line ~710)
    - `onEditEventRequested` parameter (constructor line ~282, field line ~1234)
    - `_buildDefaultShortcuts()` method (line ~5755)
    - `_buildShortcutsMap()` method (line ~5792)
    - `_buildActionsMap()` method (line ~5803)
    - `Shortcuts` and `Actions` widget wrappers in `build()` (line ~6268) — replace with direct child
  - **Update callback signatures:**
    - `onCreateEventRequested`: from `VoidCallback?` to `FutureOr<bool> Function(BuildContext, DateTime)?` — update constructor, field, dartdoc. Note in dartdoc that the return value is ignored per Requirement 8 AC 6.
    - `onDeleteEventRequested`: from `void Function(MCalCalendarEvent)?` to `FutureOr<bool> Function(BuildContext, MCalEventTapDetails)?` — update constructor, field, dartdoc
  - **Add new:**
    - `onEventTypeConversionRequested` parameter: `FutureOr<bool> Function(BuildContext, MCalCalendarEvent, bool toAllDay, DateTime? suggestedStartTime)?`
    - `keyBindings` parameter: `MCalDayKeyBindings?`
  - **Update `multi_calendar.dart`:** Remove `MCalDayViewCreateEventIntent`, `MCalDayViewDeleteEventIntent`, `MCalDayViewEditEventIntent` from the `show` clause of the `mcal_day_view.dart` export. (`MCalDayViewKeyboardMoveIntent` and `MCalDayViewKeyboardResizeIntent` are not exported currently — verify.)
  - Purpose: Clean API surface — remove legacy, align callback signatures with Month View, add Day View-specific callbacks
  - _Leverage: Month View's callback signatures in `mcal_month_view.dart` for `onCreateEventRequested` and `onDeleteEventRequested` patterns. `MCalEventTapDetails` for the delete callback._
  - _Requirements: 7.1, 7.7, 7.8, 8.2, 8.5, 9.1, 10.1, 10.2, 10.3, 6.4_
  - _Prompt: Role: Flutter Developer | Task: Perform the API migration in `lib/src/widgets/mcal_day_view.dart` as specified in this task. This is a coordinated removal + update: (1) DELETE the 5 Intent classes at the top of the file (~lines 26–77). (2) DELETE the `keyboardShortcuts` parameter from the constructor (~line 219) and its field declaration (~line 710) and all dartdoc. (3) DELETE the `onEditEventRequested` parameter from the constructor (~line 282) and its field declaration (~line 1234) and all dartdoc. (4) DELETE `_buildDefaultShortcuts()` (~line 5755), `_buildShortcutsMap()` (~line 5792), and `_buildActionsMap()` (~line 5803). (5) In `build()`, find the `Shortcuts(shortcuts: _buildShortcutsMap(), child: Actions(actions: _buildActionsMap(), child: focusContent))` block (~line 6268) and replace it with just `focusContent` — remove the `Shortcuts` and `Actions` wrappers entirely. (6) UPDATE `onCreateEventRequested` field type from `VoidCallback?` to `FutureOr<bool> Function(BuildContext, DateTime)?` with updated dartdoc noting the return value is ignored. (7) UPDATE `onDeleteEventRequested` field type from `void Function(MCalCalendarEvent)?` to `FutureOr<bool> Function(BuildContext, MCalEventTapDetails)?` with updated dartdoc. (8) ADD `final FutureOr<bool> Function(BuildContext, MCalCalendarEvent, bool toAllDay, DateTime? suggestedStartTime)? onEventTypeConversionRequested;` with dartdoc. (9) ADD `final MCalDayKeyBindings? keyBindings;` with dartdoc. (10) In `lib/multi_calendar.dart`, update the `mcal_day_view.dart` export's `show` clause: remove `MCalDayViewCreateEventIntent`, `MCalDayViewDeleteEventIntent`, `MCalDayViewEditEventIntent`. Import `dart:async` for `FutureOr` if not already imported. | Restrictions: Do NOT implement new keyboard handlers yet (that's Phase 3). The existing `_handleKeyEvent`, `_handleKeyboardMoveKey`, `_handleKeyboardResizeKey` remain unchanged for now — they will be refactored in Tasks 6–10. The existing `_enterKeyboardMoveMode` and `_enterKeyboardResizeMode` methods also remain unchanged. Do NOT change any existing usages of `onCreateEventRequested` or `onDeleteEventRequested` in the current handlers — those will be updated in Phase 3. If removing the `_buildActionsMap()` method causes compile errors in `_enterKeyboardMoveMode`/`_enterKeyboardResizeMode` calls, leave those methods in place (they are called from the handlers, not from Actions). | Success: `dart analyze` clean. `MCalDayViewCreateEventIntent` no longer exists. `MCalDayView` no longer has `keyboardShortcuts` or `onEditEventRequested` parameters. New callback parameters compile correctly._

## Phase 3: State Machine Implementation

- [ ] 6. Add state fields, refactor `_handleKeyEvent` router, add helper methods
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - **Add state fields** to `_MCalDayViewState`: `bool _isKeyboardEventMode = false`, `int? _focusedSlotIndex` (null = all-day section focused; starts as `null` which means the all-day section has initial focus when the calendar first receives keyboard focus, satisfying Req 1.2 and 2.2), `int? _lastTimeGridSlotIndex`, `bool _isKeyboardOverflowFocused = false`, `int _keyboardEventIndex = 0`.
  - **Add computed property**: `MCalDayKeyBindings get _keyBindings => widget.keyBindings ?? const MCalDayKeyBindings();`
  - **Add helper methods**:
    - `bool _matchesAny(List<MCalKeyActivator> activators, LogicalKeyboardKey key)` — extracts modifier state from `HardwareKeyboard.instance`, calls `activator.matches()` on each
    - `void _exitAllKeyboardModes()` — resets `_isKeyboardEventMode`, `_isKeyboardMoveMode`, `_isKeyboardResizeMode`, `_isKeyboardOverflowFocused`, `_focusedEvent`, `_keyboardMoveEvent`, etc. to defaults, calls `setState`
    - `void _handleDeleteResult(FutureOr<bool> result)` — mirrors Month View's pattern: sync `true` calls `_exitAllKeyboardModes`, async `true` does same after checking `mounted`
    - `void _handleConversionResult(FutureOr<bool> result)` — same pattern as `_handleDeleteResult`
  - **Refactor `_handleKeyEvent`**: Replace the current body with the four-mode router as shown in the design. The Escape handling at the top remains (works even with `enableKeyboardNavigation == false`). After the `enableKeyboardNavigation` check, dispatch to `_handleKeyboardResizeModeKey` → `_handleKeyboardMoveModeKey` → `_handleKeyboardEventModeKey` → `_handleNavigationModeKey`. Note: The existing methods `_handleKeyboardResizeKey` and `_handleKeyboardMoveKey` have not been renamed yet at this point — use their current names in the router for now. Tasks 9 and 10 will rename them to `_handleKeyboardMoveModeKey` and `_handleKeyboardResizeModeKey` respectively; update the router calls at that time. Create stub methods for `_handleNavigationModeKey` and `_handleKeyboardEventModeKey` that return `KeyEventResult.ignored` (implemented in Tasks 7–8).
  - Purpose: Foundation for the four-mode state machine with shared infrastructure
  - _Leverage: Month View's `_matchesAny`, `_handleDeleteResult`, `_exitKeyboardMoveMode` patterns in `mcal_month_view.dart`. Existing Day View `_handleKeyEvent` at line 2368._
  - _Requirements: 1.1, 1.2, 1.7, 1.8, 6.4, 6.5_
  - _Prompt: Role: Flutter Developer with expertise in key event handling | Task: Add state fields and infrastructure to `_MCalDayViewState` in `lib/src/widgets/mcal_day_view.dart`. (1) Add state fields near the existing keyboard fields (~line 1544): `bool _isKeyboardEventMode = false;`, `int? _focusedSlotIndex;` (starts as `null`, meaning the all-day section is focused by default when the calendar first receives keyboard focus — this satisfies Req 1.2 "WHEN the calendar first receives focus THEN it SHALL be in Navigation Mode" and Req 2.2 about the all-day section being a focusable slot), `int? _lastTimeGridSlotIndex;`, `bool _isKeyboardOverflowFocused = false;`, `int _keyboardEventIndex = 0;`. (2) Add computed property: `MCalDayKeyBindings get _keyBindings => widget.keyBindings ?? const MCalDayKeyBindings();`. (3) Add `_matchesAny` method matching the design doc exactly — read modifier state from `HardwareKeyboard.instance`. (4) Add `_exitAllKeyboardModes()` that resets ALL keyboard state: `_isKeyboardEventMode = false`, `_isKeyboardMoveMode = false`, `_isKeyboardResizeMode = false`, `_isKeyboardOverflowFocused = false`, `_focusedEvent = null`, `_keyboardMoveEvent = null`, `_keyboardMoveOriginalStart = null`, `_keyboardMoveOriginalEnd = null`, `_keyboardMoveProposedStart = null`, `_keyboardMoveProposedEnd = null`, `_keyboardResizeEdge = null`, `_keyboardResizeEdgeOffset = 0.0`, `_keyboardEventIndex = 0`, then `setState(() {})`. (5) Add `_handleDeleteResult` and `_handleConversionResult` following the Month View's pattern — if `result is Future<bool>`, call `.then((value) { if (mounted && value) _exitAllKeyboardModes(); })`, else if `result == true`, call `_exitAllKeyboardModes()`. (6) Refactor `_handleKeyEvent`: keep the existing Escape block at the top (including its priority order: resize > move > drag). After `if (!widget.enableKeyboardNavigation) return KeyEventResult.ignored;`, add mode dispatch: `if (_isKeyboardResizeMode) return _handleKeyboardResizeKey(event);` → `if (_isKeyboardMoveMode) return _handleKeyboardMoveKey(event);` → `if (_isKeyboardEventMode) return _handleKeyboardEventModeKey(event);` → `return _handleNavigationModeKey(event);`. Note: use the existing method names `_handleKeyboardResizeKey` and `_handleKeyboardMoveKey` here — they will be renamed to `_handleKeyboardResizeModeKey` and `_handleKeyboardMoveModeKey` in Tasks 9 and 10 respectively, and the router calls should be updated at that time. Create placeholder `_handleNavigationModeKey(KeyEvent event)` and `_handleKeyboardEventModeKey(KeyEvent event)` that return `KeyEventResult.ignored`. Move existing scroll navigation code from `_handleKeyEvent` into `_handleNavigationModeKey` temporarily so the existing behavior is preserved until Task 7 replaces it. | Restrictions: Do NOT change or rename the existing `_handleKeyboardMoveKey` or `_handleKeyboardResizeKey` methods yet (Tasks 9–10 handle that). Preserve existing Escape handling exactly. Import `MCalDayKeyBindings` from the model file. | Success: `dart analyze` clean, existing keyboard behavior unchanged (move/resize modes still work), the new mode dispatch router is in place._

- [ ] 7. Implement Navigation Mode handler (`_handleNavigationModeKey`)
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - Replace the placeholder `_handleNavigationModeKey` with the full implementation. Handle: ↑↓ (move `_focusedSlotIndex` up/down by one, with transition to/from all-day section at `null`), Home (set `_focusedSlotIndex = 0`, never all-day), End (set to last slot index), PageUp/PageDown (change display date via controller), Enter/Space (transition to Event Mode if day has events), N (call `onCreateEventRequested` with focused time — ignore return value per Req 8 AC 6), A (jump to all-day: `_focusedSlotIndex = null`), T (jump to time grid: restore `_lastTimeGridSlotIndex` or nearest-to-now slot). ←→ return `KeyEventResult.ignored`. Auto-scroll to reveal focused slot when it moves outside visible area. Announce section transitions (A/T) via `SemanticsService.sendAnnouncement` using the localization strings from Task 4.
  - Purpose: Full Navigation Mode keyboard handling
  - _Leverage: Existing scroll navigation code in `_handleKeyEvent` (arrow up/down, page up/down, home/end). `timeToOffset` for auto-scroll calculations. `_matchesAny` and `_keyBindings` from Task 6._
  - _Requirements: 2.1–2.13, 8.1, 8.3, 8.4, 8.6_
  - _Prompt: Role: Flutter Developer | Task: Implement `_handleNavigationModeKey(KeyEvent event)` in `_MCalDayViewState`. The method receives a `KeyEvent` and returns `KeyEventResult`. Extract `final key = event.logicalKey;`. Handle these in order: (1) ↑ arrow: if `_focusedSlotIndex == 0` set to `null` (all-day), else if `_focusedSlotIndex == null` do nothing (already at top), else decrement by 1. Calculate total slots as `((widget.endHour - widget.startHour) * 60) ~/ (widget.timeSlotDuration?.inMinutes ?? 15)`. Clamp index to 0..totalSlots-1. Auto-scroll via `_scrollToSlot(_focusedSlotIndex)` helper. Announce if transitioning to all-day. Return `handled`. (2) ↓ arrow: if `_focusedSlotIndex == null` set to 0 (first time slot), else increment by 1, clamp. Auto-scroll. Announce if transitioning from all-day. Return `handled`. (3) Home: `_focusedSlotIndex = 0` (first time slot, never all-day). Auto-scroll. Return `handled`. (4) End: `_focusedSlotIndex = totalSlots - 1`. Auto-scroll. Return `handled`. (5) PageUp: navigate to previous day via `controller.setDisplayDate(addDays(controller.displayDate, -1))`. Return `handled`. (6) PageDown: navigate to next day similarly. Return `handled`. (7) Enter/Space via `_matchesAny(_keyBindings.enterEventMode, key)`: check if current day has events (`_allDayEvents.isNotEmpty || _timedEvents.isNotEmpty`). If yes, set `_isKeyboardEventMode = true`, `_keyboardEventIndex = 0`, set `_focusedEvent` to first event in display order (all-day first, then timed), announce "Event mode". If no events, do nothing. Return `handled`. (8) N via `_matchesAny(_keyBindings.createEvent, key)`: compute focused time — if `_focusedSlotIndex == null`, use display date at midnight; else compute `DateTime(y, m, d, startHour, _focusedSlotIndex * slotMinutes)`. Call `widget.onCreateEventRequested?.call(context, focusedTime)` — if result is `Future`, call `.then((_) {})` to discard. Return `handled`. (9) A via `_matchesAny(_keyBindings.jumpToAllDay, key)`: set `_focusedSlotIndex = null`, announce "All-day section" using l10n. Return `handled`. (10) T via `_matchesAny(_keyBindings.jumpToTimeGrid, key)`: set `_focusedSlotIndex = _lastTimeGridSlotIndex ?? _nearestSlotToNow()`, save in `_lastTimeGridSlotIndex`, auto-scroll, announce "Time grid, {time}" using l10n. Return `handled`. (11) ← → arrows: return `KeyEventResult.ignored`. (12) Default: return `KeyEventResult.ignored`. Create helper `_scrollToSlot(int? slotIndex)` that converts slot index to pixel offset using slot height and scrolls the time grid's `ScrollController`. Create helper `int _nearestSlotToNow()` that calculates which slot index corresponds to the current time. Wrap all state changes in `setState(() {})`. Use DST-safe arithmetic from `date_utils.dart` for PageUp/PageDown. | Restrictions: Do NOT modify other mode handlers. Use `_matchesAny` for all configurable keys. Arrow keys remain hardcoded. | Success: `dart analyze` clean. ↑↓ navigate through time slots and all-day section. Home/End jump to first/last time slot. A/T jump between sections. Enter enters Event Mode. N calls create callback._

- [ ] 8. Implement Event Mode handler (`_handleKeyboardEventModeKey`)
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - Replace the placeholder `_handleKeyboardEventModeKey` with the full implementation. Build a display-order event list: visible all-day events (capped by `_allDayVisibleCount`) + overflow indicator (if overflow exists) + all timed events. Handle: Tab/↓ cycle forward through the list (wrap around), Shift+Tab/↑ cycle backward. When the overflow indicator is focused, set `_isKeyboardOverflowFocused = true` and `_focusedEvent = null`. Enter/Space: if `_isKeyboardOverflowFocused`, fire `widget.onOverflowTap` and exit all modes; else fire `onEventTap` with the selected event and exit all modes. D/Delete/Backspace: if not overflow-focused, call `onDeleteEventRequested` with `MCalEventTapDetails`, process result via `_handleDeleteResult`. X: if not overflow-focused, call `onEventTypeConversionRequested` with the event, computed `toAllDay` and `suggestedStartTime`, process result via `_handleConversionResult`. M: if not overflow-focused, call `_enterKeyboardMoveMode()`. R: if not overflow-focused, call `_enterKeyboardResizeMode()` **only if resize is enabled** (check `widget.enableDragToResize` / `_resolveDragToResize()`); if resize is disabled, absorb R with no action (per Requirement 1.5). Escape: exit to Navigation Mode. ←→: absorb. Auto-scroll to reveal selected event. Announce event title and position on each cycle.
  - Purpose: Event cycling with activation, deletion, conversion, and mode transitions
  - _Leverage: Existing `_handleTabNavigation` (line 3014) for cycling logic and `_allDayVisibleCount` awareness. `_enterKeyboardMoveMode` and `_enterKeyboardResizeMode` for mode entry. Month View's `_handleKeyboardEventModeKey` for overflow cycling pattern._
  - _Requirements: 3.1–3.14, 7.1–7.6, 9.1–9.7_
  - _Prompt: Role: Flutter Developer | Task: Implement `_handleKeyboardEventModeKey(KeyEvent event)` in `_MCalDayViewState`. Build a unified display-order list of focusable items: `final visibleAllDay = _allDayVisibleCount != null ? _allDayEvents.take(_allDayVisibleCount!).toList() : _allDayEvents;`, then `final hasOverflow = _allDayVisibleCount != null && _allDayEvents.length > _allDayVisibleCount!;`, then `final allFocusable = [...visibleAllDay, ..._timedEvents];`. The total cyclable count is `allFocusable.length + (hasOverflow ? 1 : 0)` where the overflow indicator sits at index `visibleAllDay.length` in the cycle. Track position via `_keyboardEventIndex`. Handle keys in order: (1) cycleForward/cycleBackward via `_matchesAny`: increment/decrement `_keyboardEventIndex`, wrap around using modulo. If index lands on the overflow position (`visibleAllDay.length` when `hasOverflow`), set `_isKeyboardOverflowFocused = true`, `_focusedEvent = null`. Otherwise set `_isKeyboardOverflowFocused = false`, adjust index for timed events (subtract overflow offset), set `_focusedEvent = allFocusable[adjustedIndex]`. Auto-scroll to reveal the event. Announce via l10n `announcementDayEventCycled(title, position, total)`. Return `handled`. (2) activate via `_matchesAny(_keyBindings.activate, key)`: if `_isKeyboardOverflowFocused`, fire `widget.onOverflowTap?.call()`, then `_exitAllKeyboardModes()`. Else construct `MCalEventTapDetails(event: _focusedEvent!, displayDate: controller.displayDate)`, fire `widget.onEventTap?.call(context, details)`, then `_exitAllKeyboardModes()`. Return `handled`. (3) delete via `_matchesAny(_keyBindings.delete, key)`: if `_isKeyboardOverflowFocused`, return `handled` (absorb). If `widget.onDeleteEventRequested == null`, return `handled`. Construct `MCalEventTapDetails`, call `final result = widget.onDeleteEventRequested!(context, details)`, pass to `_handleDeleteResult(result)`. Announce `announcementDayEventDeleted`. Return `handled`. (4) convertEventType via `_matchesAny(_keyBindings.convertEventType, key)`: if `_isKeyboardOverflowFocused`, return `handled`. If `widget.onEventTypeConversionRequested == null`, return `handled`. Compute `toAllDay = !_focusedEvent!.isAllDay`. Compute `suggestedStartTime`: if `toAllDay` is false (all-day→timed), use `DateTime(displayDate.year, displayDate.month, displayDate.day, DateTime.now().hour, DateTime.now().minute)` clamped to visible range; if `toAllDay` is true, use `null`. Call callback, pass result to `_handleConversionResult`. Announce `announcementDayEventConversionRequested`. Return `handled`. (5) enterMoveMode via `_matchesAny`: if not overflow-focused, call `_enterKeyboardMoveMode()`. Return `handled`. (6) enterResizeMode via `_matchesAny`: if not overflow-focused, check if resize is enabled via `_resolveDragToResize()` (same check used by `_enterKeyboardResizeMode` internally). If resize is enabled, call `_enterKeyboardResizeMode()`. If resize is disabled, absorb the key with no action (per Req 1.5: "only if resize is enabled"). Return `handled` in both cases. (7) exitEventMode via `_matchesAny(_keyBindings.exitEventMode, key)`: `_isKeyboardEventMode = false`, `_isKeyboardOverflowFocused = false`, `_focusedEvent = null`, announce "Navigation mode". `setState(() {})`. Return `handled`. (8) ← → arrows: return `handled` (absorb). (9) Default: return `handled` (absorb all keys in Event Mode per Req 1.7). Remove or refactor the old `_handleTabNavigation` method since its logic is now in this handler. | Restrictions: Use `_matchesAny` for all configurable keys. The suggestedStartTime for all-day→timed conversion must be clamped to the visible time range (`widget.startHour` to `widget.endHour`). | Success: `dart analyze` clean. Tab cycles through visible all-day events, overflow indicator, and timed events. Enter activates. D deletes with FutureOr handling. X converts event type._

- [ ] 9. Refactor Move Mode handler (`_handleKeyboardMoveModeKey`)
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - Refactor the existing `_handleKeyboardMoveKey` (line 2492) to use configurable key bindings via `_matchesAny` and add the R→Resize transition. Rename to `_handleKeyboardMoveModeKey` for consistency with the design. Handle: ↑↓ for timed events (move by time-slot increment), ↑↓ for all-day events (absorb), ←→ for day shifts using `addDays` (respects RTL), Enter/Space confirms via `_matchesAny(_keyBindings.confirmMove, key)`, Escape cancels via `_matchesAny(_keyBindings.cancelMove, key)` and returns to Event Mode (not Navigation), R switches to Resize via `_matchesAny(_keyBindings.switchToResize, key)` (cancel current move, enter Resize for same event). Auto-scroll when moved event exits visible area.
  - Purpose: Configurable Move Mode with R→Resize transition
  - _Leverage: Existing `_handleKeyboardMoveKey` (line 2492). Month View's `_handleKeyboardMoveModeKey` for the R→Resize pattern. DST-safe `addDays()` from `date_utils.dart`. `MCalDragHandler` for validation._
  - _Requirements: 4.1–4.9, 6.5_
  - _Prompt: Role: Flutter Developer | Task: Refactor the existing `_handleKeyboardMoveKey` method (~line 2492) in `_MCalDayViewState`. Rename it to `_handleKeyboardMoveModeKey` for consistency. Replace all hardcoded `LogicalKeyboardKey` checks for non-directional keys with `_matchesAny` calls: Enter → `_matchesAny(_keyBindings.confirmMove, key)`, Escape → `_matchesAny(_keyBindings.cancelMove, key)`. Add R→Resize transition: `if (_matchesAny(_keyBindings.switchToResize, key)) { _cancelKeyboardMove(); _enterKeyboardResizeMode(); return KeyEventResult.handled; }`. For directional arrows (which remain hardcoded): (1) ↑↓ on timed events: compute new proposed start/end by shifting by one time-slot increment using `DateTime(y, m, d, h, m ± slotMinutes)`. Validate via `MCalDragHandler`. Update `_keyboardMoveProposedStart/End`. Announce position. Auto-scroll. (2) ↑↓ on all-day events: return `handled` (absorb). (3) ←→: use `addDays(date, isRTL ? -1 : 1)` for Right, opposite for Left. For timed events, preserve time-of-day. For all-day events, shift date only. Validate via `MCalDragHandler`. Update proposed. Announce. (4) Escape: revert to original position (`_keyboardMoveOriginalStart/End`), set `_isKeyboardMoveMode = false`, remain in Event Mode (`_isKeyboardEventMode` stays true). Announce cancel. (5) Enter/Space: confirm the move via `MCalDragHandler.confirmDrag`, exit all keyboard modes. Announce move confirmed. Update the `_handleKeyEvent` router to call `_handleKeyboardMoveModeKey` instead of `_handleKeyboardMoveKey`. | Restrictions: Keep existing `MCalDragHandler` integration. Use DST-safe `addDays()` for day shifts. Do NOT use `Duration(days: 1)`. Arrow keys remain hardcoded. | Success: `dart analyze` clean. Move mode works with configurable confirm/cancel/switchToResize keys. R in Move Mode transitions to Resize._

- [ ] 10. Refactor Resize Mode handler (`_handleKeyboardResizeModeKey`)
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - Refactor the existing `_handleKeyboardResizeKey` (line 2532) to use configurable key bindings via `_matchesAny` and add the M→Move transition. Rename to `_handleKeyboardResizeModeKey` for consistency. Handle: S/E for edge switching via `_matchesAny`, ↑↓ for timed events (adjust edge by time-slot increment), ↑↓ for all-day events (absorb), ←→ for day shifts (adjust edge by one day using `addDays`, applies to both timed and all-day), Enter/Space confirms, Escape cancels and returns to Event Mode, M switches to Move (cancel current resize, enter Move for same event).
  - Purpose: Configurable Resize Mode with M→Move transition
  - _Leverage: Existing `_handleKeyboardResizeKey` (line 2532). Month View's `_handleKeyboardResizeModeKey` for the M→Move pattern. `MCalDragHandler` resize methods._
  - _Requirements: 5.1–5.9, 6.5_
  - _Prompt: Role: Flutter Developer | Task: Refactor the existing `_handleKeyboardResizeKey` method (~line 2532) in `_MCalDayViewState`. Rename it to `_handleKeyboardResizeModeKey` for consistency. Replace all hardcoded `LogicalKeyboardKey` checks for non-directional keys with `_matchesAny` calls: S → `_matchesAny(_keyBindings.switchToStartEdge, key)`, E → `_matchesAny(_keyBindings.switchToEndEdge, key)`, Enter → `_matchesAny(_keyBindings.confirmResize, key)`, Escape → `_matchesAny(_keyBindings.cancelResize, key)`. Add M→Move transition: `if (_matchesAny(_keyBindings.switchToMove, key)) { _cancelKeyboardResize(); _enterKeyboardMoveMode(); return KeyEventResult.handled; }`. For directional arrows (hardcoded): (1) ↑↓ on timed events: adjust the selected edge (`_keyboardResizeEdge`) by one time-slot increment. Validate via `MCalDragHandler.updateResize`. (2) ↑↓ on all-day events: absorb. (3) ←→: adjust selected edge by one day using `addDays`, respecting RTL. For timed events, this extends/shrinks the date component while preserving time. For all-day events, this extends/shrinks the date range. (4) Escape: revert, set `_isKeyboardResizeMode = false`, remain in Event Mode. (5) Enter/Space: confirm via `MCalDragHandler.confirmResize`, exit all modes. Default edge is `MCalResizeEdge.end` per Req 5.1. Announce edge switches, resize progress. Update the `_handleKeyEvent` router to call `_handleKeyboardResizeModeKey` instead of `_handleKeyboardResizeKey`. | Restrictions: Keep existing `MCalDragHandler` resize integration. Use DST-safe arithmetic. Arrow keys remain hardcoded. | Success: `dart analyze` clean. Resize mode works with configurable S/E/confirm/cancel/switchToMove keys. M in Resize Mode transitions to Move._

## Phase 4: Visual Indicators

- [ ] 11. Implement focused slot visual indicator and auto-scroll
  - File: `lib/src/widgets/mcal_day_view.dart` (modify existing)
  - Add a visual indicator in the time grid rendering for the focused slot when in Navigation Mode. When `_focusedSlotIndex != null` and `_isKeyboardEventMode == false && _isKeyboardMoveMode == false && _isKeyboardResizeMode == false` (i.e., in Navigation Mode), render a highlight on the corresponding time slot row using `focusedSlotDecoration` (if set) or `focusedSlotBackgroundColor` (if set) or a default subtle highlight. When `_focusedSlotIndex == null` (all-day section focused in Navigation Mode), add a subtle border or background to the all-day section. Ensure the focused slot indicator does not interfere with event rendering or gridlines. Add the `_scrollToSlot` helper method (if not already added in Task 7) that scrolls the time grid to reveal the focused slot.
  - Purpose: Visual feedback for keyboard navigation focus
  - _Leverage: Existing gridline rendering in `_TimeGridPainter` or `_TimeGridWidget`. `MCalDayThemeData.focusedSlotBackgroundColor` and `focusedSlotDecoration` from Task 3. Month View's focused cell indicator for pattern reference._
  - _Requirements: 2.9, 2.10_
  - _Prompt: Role: Flutter Developer with UI expertise | Task: Add a visual indicator for the focused time slot during Navigation Mode in `_MCalDayViewState`. (1) In the time grid rendering (find where gridline rows or time slot containers are built), check if we are in Navigation Mode (`!_isKeyboardEventMode && !_isKeyboardMoveMode && !_isKeyboardResizeMode`) and if the current slot index matches `_focusedSlotIndex`. If so, apply a background decoration: use `widget.theme?.dayTheme?.focusedSlotDecoration` if set, else create a `BoxDecoration(color: widget.theme?.dayTheme?.focusedSlotBackgroundColor ?? Theme.of(context).colorScheme.primary.withOpacity(0.08))`. The indicator should be a `Container` or `DecoratedBox` behind the slot content. (2) For the all-day section: when `_focusedSlotIndex == null` and in Navigation Mode, add a subtle border (e.g., `Border.all(color: Theme.of(context).colorScheme.primary.withOpacity(0.3))`) around the all-day section container. (3) Ensure `_scrollToSlot` helper exists: given a slot index, compute the pixel offset as `slotIndex * slotHeight` where `slotHeight` is `hourHeight * (slotMinutes / 60)`, then animate the scroll controller to that offset with a short duration. If the slot is already visible, do not scroll. (4) Also verify auto-scroll when `_focusedEvent` changes in Event Mode — scroll the time grid to reveal the selected timed event (convert event start time to pixel offset and scroll). | Restrictions: The indicator must not block click/tap interactions. Keep it subtle — a light background or thin border, not a heavy overlay. Do not change the rendering of events, gridlines, or time labels. | Success: When pressing ↑↓ in Navigation Mode, a visible highlight moves between time slots. When pressing A, the all-day section gets a subtle border. The highlight is not visible in Event/Move/Resize modes._

## Phase 5: Example App Updates

- [ ] 12. Update Day Features tab for new callback signatures
  - File: `example/lib/views/day_view/tabs/day_features_tab.dart` (modify existing)
  - Update existing `onCreateEventRequested` wiring from `VoidCallback` to `(BuildContext context, DateTime date) { ... return true; }`. Update existing `onDeleteEventRequested` wiring from `void Function(MCalCalendarEvent)` to `(BuildContext context, MCalEventTapDetails details) { ... return true; }`. Add `onEventTypeConversionRequested` wiring that shows a SnackBar with the conversion details and calls the appropriate controller method. Add a `_allowKeyboardCreate` toggle (alongside existing toggles). Add a `_allowKeyboardConvert` toggle. Remove `onEditEventRequested` wiring. Wire the `keyBindings` parameter (use default `MCalDayKeyBindings()`). Add necessary localization keys to example app ARB files.
  - Purpose: Demonstrate the new keyboard callback APIs in the example app
  - _Leverage: Existing `day_features_tab.dart` callback wiring patterns. Month View's Features tab (`month_features_tab.dart`) for `onDeleteEventRequested` with `FutureOr<bool>` pattern. Existing `_allowKeyboardDelete` toggle pattern._
  - _Requirements: 7.1, 8.1, 9.1, 10.4_
  - _Prompt: Role: Flutter Developer | Task: Update `example/lib/views/day_view/tabs/day_features_tab.dart` to use the new callback signatures. (1) Find the existing `onCreateEventRequested` wiring — update from `() { ... }` to `(BuildContext context, DateTime date) { SnackBarHelper.show(context, AppLocalizations.of(context)!.snackbarCreateEventOnDate(DateFormat.yMd().format(date))); return true; }`. Add a `bool _allowKeyboardCreate = false` toggle and conditionally pass `null`. (2) Find the existing `onDeleteEventRequested` wiring — update from `(event) { ... }` to `(BuildContext context, MCalEventTapDetails details) { controller.removeEvents([details.event.id]); SnackBarHelper.show(context, '...'); return true; }`. (3) Add `onEventTypeConversionRequested: _allowKeyboardConvert ? (context, event, toAllDay, suggestedStart) { SnackBarHelper.show(context, 'Convert ${event.title} to ${toAllDay ? "all-day" : "timed"}'); return true; } : null`. Add a `bool _allowKeyboardConvert = false` toggle. (4) Remove any `onEditEventRequested` wiring. (5) Add localization keys to all 5 example app ARB files for new SnackBar messages: `snackbarCreateEventOnDate`, `snackbarEventConverted`. (6) Run `flutter pub get` in `example/` to regenerate `AppLocalizations`. | Restrictions: Do NOT change unrelated callback wiring. Follow existing toggle patterns. | Success: `flutter analyze example/` clean. New toggles appear. Callbacks fire correctly._

- [ ] 13. Update Day Accessibility tab with mode-organized keyboard guide
  - File: `example/lib/views/day_view/tabs/day_accessibility_tab.dart` (modify existing)
  - File: `example/lib/l10n/app_en.arb` (and `app_ar.arb`, `app_es.arb`, `app_fr.arb`, `app_he.arb`)
  - Reorganize the keyboard shortcut guide from a flat list of legacy Cmd/Ctrl shortcuts to mode-organized sections matching the Month View's Accessibility tab pattern (using `_ModeSection` and `_ShortcutRow` or equivalent widgets). Create four mode sections: Navigation Mode (↑↓, Home, End, PgUp/PgDn, Enter/Space, N, A, T), Event Mode (Tab/↑↓, Enter/Space, D, X, M, R, Esc), Move Mode (↑↓, ←→, Enter/Space, R, Esc), Resize Mode (S, E, ↑↓, ←→, M, Enter/Space, Esc). Remove legacy Cmd/Ctrl shortcut references. Add a note about configurable key bindings via `MCalDayKeyBindings`. Add platform detection (e.g., `_supportsKeyboard` as used in the Month View's Accessibility tab) and wrap the keyboard shortcut section in `if (supportsKeyboard)` so it is hidden on iOS/Android (per NFR Usability: "On mobile platforms, the keyboard shortcut guide SHALL be hidden"). Add ~30 localization keys to all 5 example app ARB files for mode headers, descriptions, and per-shortcut action descriptions.
  - Purpose: Clear, mode-organized keyboard documentation matching the Month View pattern
  - _Leverage: Month View's Accessibility tab (`month_accessibility_tab.dart`) for `_ModeSection` and `_ShortcutRow` widget patterns. Existing Day View Accessibility tab structure._
  - _Requirements: NFR Accessibility (keyboard shortcut guide organized by mode with localized descriptions)_
  - _Prompt: Role: Flutter Developer | Task: Reorganize the keyboard shortcut guide in `example/lib/views/day_view/tabs/day_accessibility_tab.dart`. (1) Look at `example/lib/views/month_view/tabs/month_accessibility_tab.dart` for the mode-organized pattern using section widgets and shortcut row widgets. Also look at how the Month View uses `_supportsKeyboard` (based on `defaultTargetPlatform`) to conditionally show/hide the keyboard guide on mobile. (2) Add platform detection: define `final supportsKeyboard = !{TargetPlatform.iOS, TargetPlatform.android}.contains(defaultTargetPlatform);` (or equivalent, matching the Month View's pattern). Wrap the entire keyboard shortcut section in `if (supportsKeyboard)` so it is hidden on iOS/Android (per NFR Usability). (3) Replace the current keyboard shortcut section with four mode sections: Navigation Mode (8 shortcuts + note), Event Mode (8 shortcuts), Move Mode (5 shortcuts), Resize Mode (6 shortcuts). Use localized strings for mode names, descriptions, key labels, and action descriptions. (4) Remove all legacy Cmd/Ctrl shortcut references. (5) Add a configurable bindings note below the header (similar to Month View's). (6) Add ~30 localization keys to all 5 example app ARB files (`example/lib/l10n/app_*.arb`) covering mode headers (`accessibilityDayNavMode`, etc.), shortcut descriptions (`accessibilityDayNavUp`, `accessibilityDayEventTab`, etc.), and the configurable note. Translate to all 5 languages. (7) Run `flutter pub get` in `example/` to regenerate. | Restrictions: Do NOT change the rest of the Accessibility tab (screen reader guide, accessibility checklist, high contrast demo). Follow Month View's pattern closely for consistency. | Success: `flutter analyze example/` clean. Keyboard shortcut guide shows four mode sections with all 24+ shortcuts documented. Guide is not visible on iOS/Android._

## Phase 6: Testing

- [ ] 14. Unit tests for `MCalDayKeyBindings`
  - File: `test/models/mcal_day_key_bindings_test.dart` (NEW)
  - Test default constructor produces correct defaults for all 24 actions. Test `copyWith` overrides specific properties while preserving others. Test `const MCalDayKeyBindings()` compiles in a const context. Test `==` and `hashCode` for identical and differing instances. Test Day View-specific defaults: `jumpToAllDay` defaults to A, `jumpToTimeGrid` defaults to T, `convertEventType` defaults to X.
  - Purpose: Verify key bindings data model correctness
  - _Leverage: Existing `test/models/mcal_month_key_bindings_test.dart` for test patterns_
  - _Requirements: 6.1, 6.2, 6.3, 6.8_
  - _Prompt: Role: QA Engineer | Task: Create `test/models/mcal_day_key_bindings_test.dart` with comprehensive tests for `MCalDayKeyBindings`. Follow the exact pattern from `test/models/mcal_month_key_bindings_test.dart`. Test groups: (1) "default constructor" — verify all 24 properties have correct default values. Check shared defaults (enterEventMode = Enter+Space, etc.) and Day View-specific defaults (jumpToAllDay = A, jumpToTimeGrid = T, convertEventType = X). (2) "copyWith" — override one property, verify it changes while all others remain at defaults. Override multiple properties. Override with empty list to disable an action. (3) "const construction" — verify `const MCalDayKeyBindings()` compiles. (4) "equality" — verify two default instances are equal, verify instances with different overrides are not equal. (5) "Day View-specific properties" — verify `jumpToAllDay`, `jumpToTimeGrid`, `convertEventType` exist and have correct types. | Restrictions: Do NOT modify existing test files. Mirror existing test structure. | Success: All tests pass with `flutter test test/models/mcal_day_key_bindings_test.dart`._

- [ ] 15. Widget tests for Navigation Mode and Event Mode
  - File: `test/widgets/mcal_day_view_keyboard_nav_test.dart` (NEW)
  - Test Navigation Mode: ↑↓ move focused slot, ↑ from first slot focuses all-day, ↓ from all-day focuses first slot, Home/End, PageUp/PageDown, Enter/Space enters Event Mode, N triggers create callback, A/T section jumps, ←→ ignored. Test Event Mode: Tab/↑↓ cycling (including overflow indicator), Enter/Space activation, D delete with FutureOr handling, X conversion with correct toAllDay/suggestedStartTime, M→Move, R→Resize, Escape→Navigation. Test visible-only cycling with `_allDayVisibleCount`.
  - Purpose: Verify Navigation and Event Mode behavioral correctness
  - _Leverage: Existing keyboard test patterns from `test/widgets/` — controller setup, `pumpWidget` helper, key simulation. Month View keyboard tests for pattern reference._
  - _Requirements: 1.1–1.8, 2.1–2.13, 3.1–3.14, 7.1–7.6, 8.1–8.6, 9.1–9.7_
  - _Prompt: Role: QA Engineer specializing in Flutter widget testing | Task: Create `test/widgets/mcal_day_view_keyboard_nav_test.dart`. Set up a test harness: `_TestController extends MCalEventController`, `pumpCalendar` helper that pumps `MCalDayView` in `MaterialApp > Directionality > Scaffold > SizedBox(800x600)` with configurable callbacks and events. Create a `focusAndSendKey` helper. Test groups: (1) "Navigation Mode" — 12+ tests covering ↑↓ slot movement, boundary transitions (first slot ↑ → all-day, all-day ↓ → first slot), Home (→ first time slot, not all-day), End, PageUp/PageDown (verify `controller.displayDate` changes), Enter/Space with events → Event Mode, Enter/Space without events → no change, N → create callback with correct DateTime, A → all-day, T → time grid (restores last slot), ←→ → ignored. (2) "Event Mode" — 15+ tests covering Tab/↓ forward cycling, Shift+Tab/↑ backward cycling, wrap-around, overflow indicator cycling (set up many all-day events), Enter on event → onEventTap fired + exits, Enter on overflow → onOverflowTap + exits, D → delete callback with FutureOr (test sync true, sync false, async), X on timed event → toAllDay=true + suggestedStartTime=null, X on all-day event → toAllDay=false + suggestedStartTime≠null, M → Move Mode, R → Resize Mode, Escape → Navigation, ←→ absorbed. | Restrictions: Do NOT modify existing test files. Use `sendKeyEvent` or `simulateKeyDownEvent` from `flutter_test`. | Success: All tests pass with `flutter test test/widgets/mcal_day_view_keyboard_nav_test.dart`._

- [ ] 16. Widget tests for Move Mode, Resize Mode, custom bindings, and legacy removal
  - File: `test/widgets/mcal_day_view_keyboard_move_resize_test.dart` (NEW)
  - Test Move Mode: ↑↓ timed event movement, ↑↓ all-day absorbed, ←→ day shifts (with RTL), Enter/Space confirm, Escape cancel → returns to Event Mode, R → Resize transition. Test Resize Mode: S/E edge switch, ↑↓ timed edge adjustment, ↑↓ all-day absorbed, ←→ day adjustment for both timed and all-day, Enter/Space confirm, Escape cancel → Event Mode, M → Move transition. Test custom key bindings: remap an action → verify new key works, old key doesn't. Disable an action → verify no-op. Modifier-aware binding. Test legacy removal: verify `MCalDayViewCreateEventIntent` and other intents no longer exist (compile-time). Verify `keyboardShortcuts` and `onEditEventRequested` no longer exist.
  - Purpose: Verify Move/Resize Modes, custom bindings, and legacy removal
  - _Leverage: Same test harness from Task 15. `MCalDragHandler` test patterns. Month View Move/Resize tests for reference._
  - _Requirements: 4.1–4.9, 5.1–5.9, 6.4–6.7, 10.1–10.3_
  - _Prompt: Role: QA Engineer | Task: Create `test/widgets/mcal_day_view_keyboard_move_resize_test.dart`. Reuse the test harness pattern from Task 15. Test groups: (1) "Move Mode" — ↑↓ on timed event moves by slot increment (verify proposed position changes), ↑↓ on all-day event absorbed, ←→ moves by one day (test both LTR and RTL via `Directionality`), Enter/Space confirms (verify drag handler confirm called), Escape cancels (verify revert to original + returns to Event Mode not Navigation), R → transitions to Resize for same event. (2) "Resize Mode" — S switches to start edge, E to end edge, ↑↓ on timed adjusts edge, ↑↓ on all-day absorbed, ←→ adjusts edge by one day for both timed and all-day, Enter/Space confirms, Escape cancels → Event Mode, M → transitions to Move for same event. (3) "Custom key bindings" — create DayView with `keyBindings: MCalDayKeyBindings(enterMoveMode: [MCalKeyActivator(LogicalKeyboardKey.keyX)])`. Enter Event Mode, press X → verify Move Mode, press M → verify NOT handled. Test `delete: []` → D does nothing. Test modifier: `activate: [MCalKeyActivator(LogicalKeyboardKey.keyA, control: true)]` → Ctrl+A activates, plain A does not. (4) "Legacy removal" — test that `MCalDayView` constructor does not accept `keyboardShortcuts` or `onEditEventRequested` (compile-time verification via a test that constructs MCalDayView without those params). | Restrictions: Do NOT modify existing test files. | Success: All tests pass with `flutter test test/widgets/mcal_day_view_keyboard_move_resize_test.dart`._

- [ ] 17. Accessibility announcement tests
  - File: `test/widgets/mcal_day_view_keyboard_accessibility_test.dart` (NEW)
  - Test that mode transitions produce `SemanticsService.sendAnnouncement` with correct localized strings. Test event cycling announces title and position. Test section jumps (A/T) announce section name. Test delete/conversion announce results. Verify announcements use `MCalLocalizations` strings.
  - Purpose: Verify screen reader announcements for all keyboard interactions
  - _Leverage: Month View accessibility test patterns. `SemanticsService` mock/spy patterns from Flutter testing._
  - _Requirements: 1.6, NFR Accessibility_
  - _Prompt: Role: QA Engineer with accessibility testing expertise | Task: Create `test/widgets/mcal_day_view_keyboard_accessibility_test.dart`. Test that `SemanticsService.sendAnnouncement` is called with correct strings during keyboard navigation. Flutter's `TestWidgetsFlutterBinding` captures semantics announcements via `tester.takeAnnouncements()` (or check `SemanticsService` spy). Test cases: (1) Entering Event Mode announces "Event mode, N events". (2) Cycling events announces "{title}, {position} of {total}". (3) Entering Move Mode announces "Move mode for {title}". (4) Entering Resize Mode announces "Resize mode for {title}". (5) Pressing A announces "All-day section". (6) Pressing T announces "Time grid, {time}". (7) Escape from Event Mode announces "Navigation mode". (8) Delete announces "{title} deleted". (9) Conversion announces "Converting {title} to {type}". Verify all announcement strings are localized (test with a non-English locale to confirm translations are used). | Restrictions: Do NOT modify existing test files. Use Flutter's built-in semantics testing utilities. | Success: All tests pass and verify that every mode transition and action produces the correct announcement._
